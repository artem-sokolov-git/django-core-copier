from .base import *

# Database
# https://docs.djangoproject.com/en/5.2/ref/settings/#databases

DATABASES = {
    {%- if use_psql %}
    "default": {
        "ENGINE": "django.db.backends.postgresql",
        "NAME": env("POSTGRES_DB", default="{{project_slug}}_db"),
        "USER": env("POSTGRES_USER", default="postgres"),
        "PASSWORD": env("POSTGRES_PASSWORD", default="dev_psql_password_change_me"),
        "HOST": env("POSTGRES_HOST", default="{{project_slug}}_psql"),
        "PORT": env("POSTGRES_PORT", default="5432"),
    },
    {%- else %}
    "default": {
        "ENGINE": "django.db.backends.mysql",
        "NAME": env("MYSQL_DATABASE", default="{{project_slug}}_db"),
        "USER": env("MYSQL_USER", default="{{author_username}}"),
        "PASSWORD": env("MYSQL_PASSWORD", default="dev_mysql_password_change_me"),
        "HOST": env("MYSQL_HOST", default="{{project_slug}}_mysql"),
        "PORT": env("MYSQL_PORT", default="3306"),
    },
    {%- endif %}
}
